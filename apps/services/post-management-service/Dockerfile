# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build services-post-management-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t services-post-management-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system services-post-management-service && \
          adduser --system -G services-post-management-service services-post-management-service

COPY dist/apps/services/post-management-service services-post-management-service
RUN chown -R services-post-management-service:services-post-management-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix services-post-management-service --omit=dev -f install

CMD [ "node", "services-post-management-service" ]
